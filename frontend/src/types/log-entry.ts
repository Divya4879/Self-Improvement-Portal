export interface LogEntry {
    id: string
    date: string
    activity: string
    duration: number
    notes: string
    timeOfDay: string
    preActivityRating: number
    postActivityRating: number
  }